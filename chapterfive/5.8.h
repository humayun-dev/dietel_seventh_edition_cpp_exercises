/*		Program: (Find the Smallest Integer)
	    Write a program that uses a for statement to find the smallest of several integers. Assume that the first value read specifies the number of values 
		remaining.
		Author: Muhammad Humayun Khan
*/
class smallestInteger{
	private:
		int inputLimit,smallest_number,nextNumber;
	public:
		smallestInteger();			// by default constructor
		void setData();				// input and display the required data 
};
